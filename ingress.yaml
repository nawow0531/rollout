apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-app
  namespace: rollout
spec:
  ingressClassName: nginx
  rules:
  - host: sample-app.exam.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sample-app-active
            port:
              number: 80
